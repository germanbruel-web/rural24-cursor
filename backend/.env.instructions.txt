# =====================================================
# VERIFICAR VARIABLES DE ENTORNO
# =====================================================

# Tu .env.local debe tener ESTAS variables exactas:

NEXT_PUBLIC_SUPABASE_URL=https://lmkuecdvxtenrikjomol.supabase.co
SUPABASE_SERVICE_ROLE_KEY=tu_service_role_key_aqui

# IMPORTANTE: 
# - La SUPABASE_SERVICE_ROLE_KEY es diferente a la ANON_KEY
# - La consigues en: Supabase Dashboard → Settings → API → service_role key (secret)
# - NO uses la anon key para el backend, debe ser la service_role key

# Para verificar que funciona, reinicia el backend después de cambiar el .env.local
