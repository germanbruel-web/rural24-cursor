import React from 'react';

interface HowItWorksSectionProps {
  onRegisterClick?: () => void;
}

// Sección eliminada — datos hardcodeados removidos en Sprint 8C
// TODO: reconectar cuando se implemente API real de clima, precios MATBA, noticias
export const HowItWorksSection: React.FC<HowItWorksSectionProps> = () => null;
