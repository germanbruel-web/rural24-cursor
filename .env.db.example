# ============================================================
# .env.db.example → copiá este archivo como .env.db.local
# NUNCA commitear .env.db.local (tiene credenciales reales)
# ============================================================

# URL de conexión directa a Supabase DEV
# Supabase Dashboard → Settings → Database → Connection string → URI
DEV_DB_URL=postgresql://postgres:[PASSWORD]@db.[DEV-PROJECT-REF].supabase.co:5432/postgres

# URL de conexión directa a Supabase PROD
PROD_DB_URL=postgresql://postgres:[PASSWORD]@db.[PROD-PROJECT-REF].supabase.co:5432/postgres
